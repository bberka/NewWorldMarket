@model NewWorldMarket.Core.Models.UploadImage
@{
    ViewData["Title"] = "Upload Item Image";
}

<div class="container ">
    <h1 class="text-light">Upload Item Image</h1>

    <hr class=" text-light"/>
    <div class="row">
        <div class="col-md-4">
            <form class="col-xs-12" method="post" asp-action="UploadItemImage" enctype="multipart/form-data">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group text-light">
                    <label>Things to know before uploading item screenshot;</label>
                    <ul class="px-3">
                        <li class="py-1">
                            1. Your game language must be set to English.
                        </li>
                        <li class="py-1">
                            2. Colorblind mode should be off. 
                        </li >
                        <li class="py-1">
                            3. Item image should be taken in a way that it is not covered by any other UI elements.
                        </li>
                        <li class="py-1">
                            4. All item data should be in the image.
                        </li>
                        <li class="py-1">
                            5. Take the picture in a way that is only in item borders and not outside of it. Be sure no other data is in the image.
                        </li>
                        <li class="py-1">
                            6. Be sure the item you want to sell is "Bind on Equip" and it is not bound to you. 
                        </li>
                        <li class="py-1">
                            7. Example of a good image;  <a asp-controller="Home" asp-action="Example">Screenshot examples</a>
                        </li>

                    </ul>
                </div>
                <div class="form-group">
                    <input type="file" asp-for="FormFile" class="form-control"/>
                </div>

                <div class="form-group pt-3">
                    <button type="submit" class="btn btn-primary col-xs-12">Upload</button>
                </div>
            </form>
        </div>
    </div>

</div>